require "spec_helper"

RSpec.describe MultiplicationTable do
  it "has a version number" do
    expect(MultiplicationTable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
